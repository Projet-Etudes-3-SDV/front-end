export enum SubscriptionPlan {
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
  FREE_TRIAL = 'free-trial',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
  TRIAL = 'trial',
}
